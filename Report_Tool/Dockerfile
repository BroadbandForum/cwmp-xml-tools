# official minimal Ubuntu release
# XXX is this guaranteed to be ubuntu:latest?
FROM ubuntu

# set the working directory to /report-tool
WORKDIR /report-tool

# copy the current directory contents into the container at /report-tool
ADD . /report-tool

TBD
